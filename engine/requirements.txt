python-docx==1.1.2
joblib==1.4.2
numpy>=2.0.0
scikit-learn==1.6.1

# scikit-learn==X.Y.Z   <-- REQUIRED once model/artifacts/classifier.joblib exists.
#                            Copy the exact version from model/requirements.txt
#                            (generated by Person A's train.ipynb, Section 6) --
#                            do NOT just `pip install scikit-learn` with no
#                            version pin, a mismatch will silently degrade
#                            or break predictions.
#
# Until the trained model is dropped in, this engine runs fine WITHOUT
# scikit-learn installed at all -- classifier_stub.py falls back to the
# rule-based stub automatically.
python-docx==1.2.0
joblib==1.6.0
numpy==2.1.3

# scikit-learn MUST match the exact version Person A trained with -- do NOT
# bump this to "latest" just because it's older. classifier.joblib was
# pickled with 1.6.1; loading it with a different major/minor version
# prints InconsistentVersionWarning and can silently change predictions.
# Verified during Phase 2 integration: loading with 1.8.0 installed throws
# this exact warning; pinning to 1.6.1 loads clean with no warnings.
scikit-learn==1.6.1

# If Person A retrains with a newer scikit-learn later, update this pin to
# match and re-verify with:
#   python -c "import sys; sys.path.insert(0,'engine'); from classifier_stub import load_real_model; load_real_model()"
# -- if you see InconsistentVersionWarning, the pin is wrong, not the code.
